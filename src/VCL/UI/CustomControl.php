<?php

declare(strict_types=1);

namespace VCL\UI;

/**
 * CustomControl is the base class for custom controls.
 *
 * This class serves as a foundation for creating custom controls with
 * additional painting and layout capabilities. Currently it doesn't
 * implement any specific properties or methods, but provides a clear
 * inheritance point for future extensions.
 *
 * PHP 8.4 version.
 */
class CustomControl extends FocusControl
{
    // Reserved for future implementation of custom control features
    // such as custom painting, double buffering, etc.
}
